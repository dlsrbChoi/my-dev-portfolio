{
  "buildCommand": "npm run build && node scripts/create-middleware-nft.js || true",
  "installCommand": "npm ci --legacy-peer-deps",
  "env": {
    "NEXT_SKIP_ENV_VALIDATION": "true",
    "VERCEL_SKIP_TURBOPACK": "true"
  },
  "outputDirectory": ".next"
}
